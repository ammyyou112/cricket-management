# =============================================================================
# CRICKET 360 - Production Environment Variables
# =============================================================================
# ⚠️ PRODUCTION CONFIGURATION - Use strong, unique values!
# Copy this file to .env.production and update with your production values
# =============================================================================

# =============================================================================
# Server Configuration
# =============================================================================
NODE_ENV=production
PORT=3000
HOST=0.0.0.0
API_PREFIX=/api/v1

# =============================================================================
# Database Configuration
# =============================================================================
# Use your PRODUCTION Supabase database credentials
# ⚠️ NEVER use development database in production!

DATABASE_URL="postgresql://postgres:[PRODUCTION-PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:[PRODUCTION-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# =============================================================================
# JWT Authentication (PRODUCTION SECRETS)
# =============================================================================
# ⚠️ CRITICAL: Use strong, unique secrets (minimum 64 characters recommended)
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Store securely and rotate periodically

JWT_SECRET=your-production-jwt-secret-minimum-64-characters-very-secure-change-this-immediately
JWT_REFRESH_SECRET=your-production-refresh-secret-minimum-64-characters-very-secure-change-this-too
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=7d

# Production token expiration (shorter for security):
# - 1d = 1 day (access token)
# - 7d = 7 days (refresh token)

# =============================================================================
# Supabase Configuration (PRODUCTION)
# =============================================================================
SUPABASE_URL=https://[PROJECT-REF].supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key
SUPABASE_STORAGE_BUCKET=cricket360-uploads

# =============================================================================
# CORS & Frontend Configuration (PRODUCTION)
# =============================================================================
# ⚠️ Update with your actual production domains
# Remove localhost URLs in production

CORS_ORIGIN=https://your-frontend-domain.com
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com
FRONTEND_URL=https://your-frontend-domain.com

# =============================================================================
# File Upload Configuration
# =============================================================================
# Production: Allow larger files (10MB)
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp

# =============================================================================
# Rate Limiting (STRICTER FOR PRODUCTION)
# =============================================================================
# Production: Stricter rate limits to prevent abuse
RATE_LIMIT_WINDOW_MS=600000
RATE_LIMIT_MAX_REQUESTS=50

# Production rate limit:
# 600000 ms = 10 minutes
# 50 requests per 10 minutes per IP address

# =============================================================================
# Logging Configuration (PRODUCTION)
# =============================================================================
# Production: Only log errors and warnings to reduce log volume
LOG_LEVEL=error
LOG_FILE=./logs/app.log

# Production log levels:
# - error: Only errors (recommended)
# - warn: Warnings and errors
# - info: Info, warnings, and errors (if you need more visibility)
